import 'package:flutter/material.dart';

class FormSettingsVolleyball extends StatelessWidget {
  const FormSettingsVolleyball({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Configurações"),
      ],
    );
  }
}
